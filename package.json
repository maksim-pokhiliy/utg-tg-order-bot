{
  "name": "telegram-bot-server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Telegram order relay for the Ukrainian Tactical Gear shop: accepts the checkout payload and delivers one formatted order message to the operators' chat.",
  "license": "MIT",
  "author": "Maksim Pokhiliy",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "smoke": "node scripts/load-smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "^24",
    "prettier": "^3.3.3",
    "typescript": "^5",
    "vitest": "^3.2"
  }
}
