# bot-polish — deferred

Carry-forwards: findings/obligations not yet scheduled, with disposition + status.
**Promote here at every gate** — a WARNING that lives only in scratch is not durable and
gets lost.

**Status:** `OPEN` (live) · `SCHEDULED` (assigned to a step) · `CLOSED` (done — kept for
the trail) · `DROPPED` (decided not to).

| ID     | One-liner                                                                                                          | Disposition                                                                                  | Status    |
| ------ | ------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- | --------- |
| BDEF-1 | The relay accepts unauthenticated POSTs until the shop sends `x-relay-secret` and both Vercel envs are set (BD-4) | **CLOSED 2026-08-06** — B2 shipped as ua-checkout step U0 (shop PR #20 `bb3f866`), the secret is set on both Vercel projects (Production, Sensitive) and the relay redeployed to bind it. Verified live: 401 with no header and with a wrong header, 400 with the correct one, and an authenticated probe through the prod shop route came back 400 (validation) rather than 401 — the chain holds. `DEF-13` closed in the shop ledger the same day | CLOSED |
| BDEF-2 | Module-hygiene batch from the B1 re-review (RF-27…RF-36, RF-40), NUMBERED so later steps can cite items: **(1)** dead/write-only surface (`countCodePoints` export, `SendFailure.reason`, `createTimeoutSignal`); **(2)** untyped `readEnv` name — a typo silently disables auth; **(3)** the deployed `tsconfig` including test paths; **(4)** the TDZ hazard in the file's lone `function` declaration; **(5)** `.smoke-build` left behind on a failed compile; **(6)** both request stubs overriding `json()`, so the real parse path is untested; **(7)** same as (6) for the remaining stubs. B3 took (4) and part of (6), and retired (1) as stale — `countCodePoints` gained four test consumers, so the premise expired. **(2) CLOSED by B5** (the union type made the typo a compile error — and B5 raised its stakes first: a typo'd `DATABASE_URL` read would have left the store permanently dark with every test green). **Added by the B5 review** (RF-14/RF-16, none a defect): **(8)** `isTimeout` duplicated between `src/telegram.ts` and `src/neon.ts`, plus `readErrorCode`/`readRows` sharing a 6-line skeleton and `logFailure` ≡ `logEvent`; **(9)** `readTelegramCalls` re-implements `readSentMessage`; **(10)** `store-budget.test.ts` duplicates `vercel-config.test.ts` navigation | None is a defect today — reviewer-verified, all cosmetic or unreachable. Batch them into the next bot window (U6, which also drops v1) rather than growing an already-large PR | OPEN |
| BDEF-3 | No idempotency: an ambiguous upstream outcome (HTTP 200 with an unreadable verdict, or a timeout after Telegram accepted) is reported as failure, so a buyer retry can duplicate a delivered order | B1 makes the state DIAGNOSABLE (a distinct log event, RF-22) but not preventable; real dedup needs an idempotency key in the payload — a shop+bot contract change. NOT folded into B3 (2026-08-06): the key is additive to the v2 envelope with no version bump, and B3 gates the whole shop initiative, so it stays small. **Correction the same day:** the planner had justified this with "the relay has no database, that's a project constraint" — false, and retired. No document in THIS repo says it; the shop's `no database` line describes its static catalog, and `any database` is a non-goal of the shop's ua-checkout initiative, not a law over the relay. The owner has managed Postgres (Neon, paid) available. Real dedup is therefore buildable — see the separate persistence question, where the stronger motive is not dedup but that **a delivered order is durable nowhere today**: if Telegram delivery fails or a chat dies (it already did once — B1 journal), the order is gone with no replay path. **CLOSED 2026-08-18 by B5** (PR #4 `1d31e20`): every decoded order is durable in Neon before the send and replay is a documented SELECT; the confirmed-delivered duplicate class is suppressed (proven live — a byte-identical retry answered 200 with no second message and `dedupe_of` set); the AMBIGUOUS class deliberately still re-sends (BD-11 — a suppressed undelivered order would be a silent loss, a duplicate message is a phone call), now with both attempts durable and correlated by key for reconciliation | CLOSED |

| BDEF-4 | The 4096-character Telegram budget was computed in CODE POINTS against a ceiling whose unit nobody had verified. **Read the disposition before the numbers: the "we are losing orders" premise was FALSIFIED by a live probe, and this row is kept as the trail of how.** | PRE-EXISTING on master (the B3 review surfaced it, did not introduce it). **Corrected 2026-08-08 by planner measurement on master:** the original wording blamed "an emoji-heavy order", which is wrong and understated it. The undercount is contributed by OUR OWN emoji labels -- surrogate pairs worth roughly 3 unaccounted UTF-16 units per rendered cart line and 10 for the header -- so a plain Ukrainian order with nothing exotic in any field sits 4 units under the limit at cart saturation (4092), goes OVER with short titles (4153 at 60 items) and over again with one astral character per title (4203). v2 measures 7150 (+74%). **FALSIFIED the same day by the B4 review and a live probe:** the Bot API applies 4096 to the text AFTER entities parsing (TDLib counts code points there), and ~980 units of our message are `<b>` markup that parsing consumes. A live order at 4178 raw UTF-16 / 3419 parsed code points was DELIVERED (relay 200). So raw UTF-16 is not the enforced metric, every figure above measures something Telegram does not check, and no order was being lost at these sizes. The reorder ahead of B5 was a planner error made on that measurement -- persistence was the more urgent step. What still justifies the fix: raw UTF-16 upper-bounds all four candidate metrics, so it is the only accounting provably safe under every reading, at a cost of 0-1 cart lines; the old raw-code-point budget is unsafe if the ceiling is parsed UTF-16. Still open: raw code points vs parsed length cannot be discriminated through the relay (prod truncation can never emit over 4096 raw code points) -- it needs a direct Telegram API call with the bot token, and settling it would reclaim ~980 units of cart room. **CLOSED 2026-08-08 by B4** (PR #3 `7594e94`): the accounting is unified on raw UTF-16, proven by 120k randomized payloads and mutation-pinned | CLOSED |
| BDEF-5 | Characters that mislead the operator survive into the message. Two families: bidi/zero-width controls, and (added by the B3 review, RF-4) Unicode math-bold, which passes HTML escaping untouched, so a line typed into the free-text comment renders visually bold and can imitate the genuine `Address Source` line rendered above it -- the "genuine bold line" is therefore NOT a forgery guard, and the docs no longer claim it is. Bidi example: a warehouse label carrying an embedded RLO renders with its digits reversed, so branch "No. 43" reads as "No. 34" and a parcel can be shipped to a branch nobody chose | PRE-EXISTING on master (same review). Same reason to defer as BDEF-4 -- stripping them changes v1 output. Rides the same step: one sanitizer over payload-derived strings only (well-formedness, NFKC to fold math-bold and fullwidth lookalikes, `\p{Cf}` strip), closing both the width accounting and the control-character set. **The corpus is NOT re-cut**: it keeps its legacy pin and gains a second named divergence, with exactly two of twelve entries moving (`lone-surrogate-and-astral`, `truncated-60-item-cart`). Note what the step EARNS: once math-bold cannot survive a field, bold in an order message means the relay wrote it, so the genuine `Address Source` line becomes a real forgery guard rather than a disclaimed one. **CLOSED 2026-08-08 by B4** (PR #3 `7594e94`): one sanitizer over payload-derived strings only; the matrix is driven through all 30 user-controlled fields of both versions; the corpus kept its legacy pin with one named divergence, eleven of twelve byte-identical; prod-smoked | CLOSED |

| BDEF-6 | Structural tails the v2 work left behind, all cheap and none load-bearing today (B3 review RF-8): the v1 path routes through modules named `payloadV2`/`messageV2`; `RejectReason` lives in `payload.ts` while carrying v2 members; ~16 lines of tail validation (locale -> total -> currency -> cart) are a verbatim copy between `payload.ts` and `payloadV2.ts` | The natural window is the follow-up that DROPS v1: that step deletes the v1 decoder anyway, so the naming, the union's home and the duplication all resolve as a side effect instead of as churn now. Until then the copy is documented as a copy (the PR body was corrected -- it had claimed shared code) | SCHEDULED |

| BDEF-7 | B4 review carry-forwards, none a defect today: **(1)** pre-slice starvation -- `boundWork` slices raw units before the shrinking sanitizer, so a field of 1000 zero-width characters renders empty with no ellipsis (master rendered 200 invisible characters plus a marker; not an information regression, but the README's "the slice is only a work bound" framing is incomplete); **(2)** the `\p{Cf}` strip also removes legitimate script characters -- Arabic number sign U+0600, end-of-ayah U+06DD, Syriac U+070F, Kaithi U+110BD, and ZWNJ U+200C which is orthographically mandatory in Persian and several Indic scripts; **(3)** a 16-item tail of test/naming nits enumerated in the B4 review (alias duplication, three dead exports in `tests/support/saturation.ts`, two corpus test names overclaiming their bodies, a self-referential set-size assertion, an untested `rendered.length === 0` branch) | (1) and (2) are theoretical for a uk/en storefront and both are deliberate consequences of a security control -- documenting beats loosening. (3) is hygiene. Natural window: the step that DROPS v1, alongside BDEF-6 | OPEN |

| BDEF-8 | The enforced Telegram metric is still unknown between two candidates: raw code points (which the pre-B4 code respected) or parsed length. The live probe eliminated raw UTF-16 only -- prod truncation can never emit a message over 4096 raw code points, so the relay cannot discriminate the remaining two | Settling it needs a direct Telegram API call with the bot token on a scratch chat: one message at raw 5000 / parsed 4090, one at 3000 astral code points. Worth doing because a parsed-length budget would reclaim roughly **980 units of cart room** -- real operator value, since truncation silently drops order lines. Do NOT loosen the budget on the hypothesis alone: the permissive direction costs a lost order, and there is no replay path until B5 | OPEN |

| BDEF-9 | **B5 must not deduplicate on `idempotency_key` alone.** The shop mints the key on first submit and resets it only on success (D-11), which means the key deliberately SPANS an order the buyer edited between retries — remove a cart line after an ambiguous failure and the corrected order arrives under the original key | Ruled shop-side as D-13 (2026-08-08), verified by execution during the U5a review in jsdom and in real Chromium: same key, `total` 1300.00 then 300.00, two lines then one. Live impact today is zero because nothing reads the key. But key-only dedupe in B5 would answer 200 to an order that was never delivered, and the shop would show the success screen and call `clear()` — the buyer loses their cart believing the CORRECTED order was accepted. Re-minting on cart change is NOT the fix: it resurrects the exact duplicate the key exists to prevent. **Dedupe on a content hash within a time window; treat the key as a hint, never as an identity.** **CLOSED 2026-08-18 by B5 as executed law** (BD-11): the hash leads, the key only corroborates, and the exact D-13 scenario was smoked LIVE on production — the edited cart under the reused key was DELIVERED (message 13) while its byte-identical predecessor retry was suppressed; a mutation gate pins key-only dedupe as a red test forever | CLOSED |

| BDEF-10 | **B5 created a PII datastore with no retention.** Buyer name, phone and address now live in Neon `orders.payload` where they previously existed only in the operators' chat; rows accumulate forever (R8 from the B5 executor: likelihood certain, impact grows with time). Related riders: `dedupe_of` carries no FK (deliberate — an FK adds an error class on the audit path), so a future retention sweep must not orphan the back-reference the replay queries key off | Owner accepted the datastore in the B5 contour; retention/erasure policy is deliberately out of B5 scope. Decide a policy (age-based delete beats row caps for an audit) in whatever window touches ops next; the volume is tiny, so this is about principle and exposure, not quota | OPEN |
| BDEF-11 | **The relay authenticates to Neon as the schema owner.** The role in `DATABASE_URL` can `drop table orders`; it needs only INSERT/SELECT/UPDATE on `orders` (B5 review RF-8). The migration issues no `grant`. Post-B5 the exfil path is pinned to `.neon.tech`, so the residual risk is blast radius, not leakage | Planner ops, near-term: create a scoped role in Neon, grant the three rights + sequence usage, swap `DATABASE_URL` (Sensitive/Production), redeploy the deployment ACTUALLY serving production (the BDEF-1 lesson), smoke one order. Pairs naturally with the owner's password-rotation decision since both rewrite the env value | OPEN |
| BDEF-12 | **Shop-side exposure, found by the B5 review reading the shop's source** (belongs in the shop ledger — promote at the next shop session): the forwarding route `/api/place_order` is public, excluded from the shop's proxy matcher, attaches `ORDER_RELAY_SECRET` for ANY caller, has no abort signal and no `maxDuration`, and its rate limiter is an in-memory per-lambda Map (5 req/60 s per instance). The relay's own auth therefore gates nothing for traffic that walks in through the shop | The relay side is defended (validation, size cap, fail-open store); the remaining questions — origin checks, a real rate limit, an abort signal on the forward — are shop decisions for the U6 window. Cross-repo flag only; nothing here blocks the relay.  **PROMOTED 2026-08-18 to the shop ledger as UAC-25** (`../utg-2.0/initiatives/ua-checkout/deferred.md`), where every claim was re-verified against that repo's source before it was written down: the route carries no `signal`, no `AbortController`, no timeout and no `maxDuration`, and the proxy matcher is `["/((?!_next|api).*)"]`. It is scheduled into the shop's U6 window. This row stays for the trail; the live obligation now lives in the shop's ledger | CLOSED |


## Detail on the live ones

**BDEF-1 (closed).** The BD-4 order held exactly as written. One correction learned in
execution and worth keeping: a Vercel env var binds only to the NEXT deployment, so
setting it is not enabling it — each project needs a redeploy. And redeploy the
deployment that is ACTUALLY serving production, resolved by id from its logs
(`branch=master`), never the first URL in `vercel ls --prod`: the planner redeployed by
list position, landed on a pre-B1 legacy build, and every `POST /place_order` answered
500 for ~3 minutes until `vercel promote` restored it. Runtime logs over the following
six hours showed only the planner's own ten probes and zero 200s, so no real order was
lost — but the shop was one customer away from a silent outage.

**BDEF-3.** The harm is bounded — a duplicate order the operators can reconcile by
phone, versus the alternative (reporting success on an unconfirmed send) which loses
orders silently. Fail-closed is the right default; only the diagnosis was missing.
If persistence lands, dedup comes nearly free (a unique key over the idempotency token,
or a content hash within a time window) — but the design rule is absolute: the store
must never gate the Telegram send. A database that is down must cost an audit row, never
an order.

## Closed history

(none yet)
